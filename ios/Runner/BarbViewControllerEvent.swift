
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let main_rootSourceState:String = "getDevfrom auto"

/*: "getFirebaseID" :*/
fileprivate let routerGlistenValue:[Character] = ["g","e","t","F","i","r"]
fileprivate let modelObserverStr:[Character] = ["e","b"]
fileprivate let user_fromTotalMessage:String = "aseIDin enter flexible first stop"

/*: "getAreaISO" :*/
fileprivate let dataDisplayAlwaysSuperTempKey:String = "getAprocessing please other"
fileprivate let notiObtrudeUponTopName:String = "request"

/*: "getProxyStatus" :*/
fileprivate let sessionFromDate:String = "foundation revenuegetPro"
fileprivate let formatterMediaYourID:[Character] = ["x"]
fileprivate let data_padKey:String = "yStatusbenefit search at domain"

/*: "getMicStatus" :*/
fileprivate let factoryCancelKey:String = "getMifloat package ok pass layer"
fileprivate let routerMaximumGlobalStr:String = "from docStatus"

/*: "getPhotoStatus" :*/
fileprivate let main_revenueFileMessage:String = "human device tag newgetPh"
fileprivate let parserDiskHideMode:String = "tushow"

/*: "getCameraStatus" :*/
fileprivate let k_topologicalSpaceUrl:String = "getCamtrue poor"
fileprivate let networkHumanState:String = "atlayers"

/*: "reportAdjust" :*/
fileprivate let constGlassyState:[Character] = ["r","e","p","o","r","t","A","d","j","u"]
fileprivate let formatterPointUrl:[Character] = ["s","t"]

/*: "requestLocalPush" :*/
fileprivate let routerValueStr:[Character] = ["r","e","q","u","e","s","t","L","o","c","a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let noti_textList:[Character] = ["g","e","t","L","a","n","g","C","o","d"]
fileprivate let show_speakPath:[Character] = ["e"]

/*: "getTimeZone" :*/
fileprivate let formatterConfirmSecret:String = "G"
fileprivate let k_yourUrl:String = "photo positive maximum pic thirdetTim"

/*: "getInstalledApps" :*/
fileprivate let loggerNetworkDisplayResult:[Character] = ["g","e","t","I","n","s","t","a","l","l","e","d","A","p","p","s"]

/*: "getSystemUUID" :*/
fileprivate let controllerEnvironmentActiveTime:[Character] = ["g","e","t","S","y","s","t","e"]
fileprivate let app_hellEnableEarthCount:[Character] = ["m","U","U","I","D"]

/*: "getCountryCode" :*/
fileprivate let cacheReduceMode:String = "presentation componentgetC"
fileprivate let const_factoryResult:String = "ouncurrentry"

/*: "inAppRating" :*/
fileprivate let mainTransformResult:String = "inApmessage label"

/*: "apPay" :*/
fileprivate let factoryMaskName:String = "until exclude take snap activeapPay"

/*: "subscribe" :*/
fileprivate let serviceStateValueTitle:String = "presentationubpresentation"

/*: "openSystemBrowser" :*/
fileprivate let dataOffPleaseFormat:String = "closepen"
fileprivate let kCoreProtectionVersion:String = "inform visible contribute grantmBrowse"
fileprivate let formatterTrackElementMessage:String = "dismiss"

/*: "closeWebview" :*/
fileprivate let user_buttonApplicationKey:[Character] = ["c","l","o","s","e"]
fileprivate let constBlackURL:[Character] = ["W","e","b","v","i","e","w"]

/*: "openNewWebview" :*/
fileprivate let data_languageItemMessage:String = "confirm need evaluate for categoryopenNe"
fileprivate let dataFormatKey:String = "wWebviewphoto inform"

/*: "reloadWebview" :*/
fileprivate let const_indicatorToken:[Character] = ["r","e","l","o","a","d","W","e","b","v","i","e"]
fileprivate let app_directString:[Character] = ["w"]

/*: "typeName" :*/
fileprivate let networkPresentationValue:[UInt8] = [0x9e,0x93,0x9a,0x8f,0xa4,0x8b,0x87,0x8f]

private func productionFormat(capture num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "deviceID" :*/
fileprivate let appPackageStatus:[UInt8] = [0x44,0x49,0x65,0x63,0x69,0x76,0x65,0x64]

/*: "fireBaseID" :*/
fileprivate let cacheLaterFrameMsg:[UInt8] = [0x6d,0x62,0x79,0x6e,0x49,0x6a,0x78,0x6e,0x42,0x4f]

private func makeWith(carrier num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "areaISO" :*/
fileprivate let app_neverName:[UInt8] = [0x3a,0x29,0x3e,0x3a,0x12,0x8,0x14]

private func fieldNative(search num: UInt8) -> UInt8 {
    return num ^ 91
}

/*: "isProxy" :*/
fileprivate let factoryFormatId:[UInt8] = [0xc,0x16,0x35,0x17,0xa,0x1d,0x1c]

private func trestleBridge(body num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "langCode" :*/
fileprivate let serviceGoingPhoneTopDict:[UInt8] = [0x9e,0x93,0x9c,0x95,0xb1,0x9d,0x96,0x97]

private func contributeApplication(component num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "timeZone" :*/
fileprivate let app_legalDocumentURL:[UInt8] = [0x5d,0x40,0x44,0x4c,0x73,0x46,0x47,0x4c]

private func numberContact(mirror num: UInt8) -> UInt8 {
    return num ^ 41
}

/*: "installedApps" :*/
fileprivate let constPackageKitPath:[UInt8] = [0xef,0xe8,0xf5,0xf2,0xe7,0xea,0xea,0xe3,0xe2,0xc7,0xf6,0xf6,0xf5]

/*: "systemUUID" :*/
fileprivate let cacheAgentPath:[UInt8] = [0x65,0x6f,0x65,0x62,0x73,0x7b,0x43,0x43,0x5f,0x52]

private func sizeCore(disappear num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "countryCode" :*/
fileprivate let user_captureValue:[UInt8] = [0x92,0x9e,0x84,0x9f,0x85,0x83,0x88,0xb2,0x9e,0x95,0x94]

private func centralismCan(point num: UInt8) -> UInt8 {
    return num ^ 241
}

/*: "status" :*/
fileprivate let cacheActivityString:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x73]

/*: "isAuth" :*/
fileprivate let main_textDecideData:[UInt8] = [0x33,0x3d,0xb,0x3f,0x3e,0x32]

fileprivate func presentTun(due num: UInt8) -> UInt8 {
    let value = Int(num) - 202
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isFirst" :*/
fileprivate let routerBenefitName:[UInt8] = [0x3c,0x26,0x13,0x3c,0x27,0x26,0x21]

private func totalOf(global num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: __LocalPush" :*/
fileprivate let mainSmartFormat:String = "__Locablack case copy"

/*: "identifier" :*/
fileprivate let parserMessageValue:[UInt8] = [0x3c,0x31,0x30,0x3b,0x21,0x3c,0x33,0x3c,0x30,0x27]

private func grantSystem(kind num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "HTTPProxy" :*/
fileprivate let showNeedClearHeritageMessage:String = "confirmT"
fileprivate let k_barPadMode:String = "PProxybase all"

/*: "HTTPSProxy" :*/
fileprivate let k_nurseLogTime:String = "HTTwithSwith"

/*: "SOCKSProxy" :*/
fileprivate let applicationFlagFieldState:[Character] = ["S","O","C"]
fileprivate let networkDelayEstablishResult:[Character] = ["K","S","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let app_darkName:String = "mode back temp view prompt__SCOPE"
fileprivate let notiAreaId:String = "D__allow as can first delay"

/*: "tap" :*/
fileprivate let viewAddMode:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let factoryPastDate:String = "sinceun"

/*: "ipsec" :*/
fileprivate let helperAgentDate:String = "itosec"

/*: "ppp" :*/
fileprivate let viewItemList:[Character] = ["p","p","p"]

/*: "Retry After or Go to " :*/
fileprivate let modelLabelId:String = "text let extension minimum postRe"
fileprivate let serviceGlassesKey:String = "try Afraw to"
fileprivate let showLogFirstState:String = "r Go to run trust"

/*: "Feedback" :*/
fileprivate let mainPositiveSecret:[Character] = ["F","e","e","d","b","a","c","k"]

/*: " to contact us" :*/
fileprivate let factoryClearVerticalDict:String = " to normal appear core pass"
fileprivate let configProductFileString:String = "as all take addact "
fileprivate let app_promptToolMessage:String = "usince"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let constFormatSecret = (String(main_rootSourceState.prefix(6)) + "iceID") // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let sessionMessageVersion = (String(routerGlistenValue) + String(modelObserverStr) + String(user_fromTotalMessage.prefix(5))) // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let cacheDoingAtHandID = (String(dataDisplayAlwaysSuperTempKey.prefix(4)) + "reaI" + notiObtrudeUponTopName.replacingOccurrences(of: "request", with: "SO")) // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let mainTransportServerMaximumNetFlag = (String(sessionFromDate.suffix(6)) + String(formatterMediaYourID) + String(data_padKey.prefix(7))) // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let parserCountoItemToken = (String(factoryCancelKey.prefix(5)) + String(routerMaximumGlobalStr.suffix(7))) // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let routerDueKey = (String(main_revenueFileMessage.suffix(5)) + "otoSta" + parserDiskHideMode.replacingOccurrences(of: "show", with: "s")) // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let constTextPushMode = (String(k_topologicalSpaceUrl.prefix(6)) + "eraSt" + networkHumanState.replacingOccurrences(of: "layer", with: "u")) // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let modelStatusTransportResult = (String(constGlassyState) + String(formatterPointUrl)) // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let show_usurpProcessingString = (String(routerValueStr)) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let user_linkValue = (String(noti_textList) + String(show_speakPath)) // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let parserMilieuFormat = (formatterConfirmSecret.lowercased() + String(k_yourUrl.suffix(5)) + "eZone") // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let networkSounderStr = (String(loggerNetworkDisplayResult)) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let loggerMeFormat = (String(controllerEnvironmentActiveTime) + String(app_hellEnableEarthCount)) // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let formatterInformValue = (String(cacheReduceMode.suffix(4)) + const_factoryResult.replacingOccurrences(of: "current", with: "t") + "Code") // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let factoryCornerUrl = (String(mainTransformResult.prefix(4)) + "pRating") // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let dataPositiveZoneValue = (String(factoryMaskName.suffix(5))) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let parserSupremePathMsg = (serviceStateValueTitle.replacingOccurrences(of: "presentation", with: "s") + "cribe") // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let routerActivityToken = (dataOffPleaseFormat.replacingOccurrences(of: "close", with: "o") + "Syste" + String(kCoreProtectionVersion.suffix(7)) + formatterTrackElementMessage.replacingOccurrences(of: "dismiss", with: "r")) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let kTotalTime = (String(user_buttonApplicationKey) + String(constBlackURL)) // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let k_versionUrl = (String(data_languageItemMessage.suffix(6)) + String(dataFormatKey.prefix(8))) // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let data_domainSecret = (String(const_indicatorToken) + String(app_directString)) // 重载webView

//: struct JSMessageModel: HandyJSON {
struct RaceModel: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: PingRequireWait?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct PingRequireWait: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension BarbViewController {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func prevalenceError(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = RaceModel.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case constFormatSecret:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = PrepareEvery.to()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: appPackageStatus.reversed(), encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case sessionMessageVersion:
                //: AppWebViewController.getFireBaseToken { str in
                BarbViewController.outsideObject { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cacheLaterFrameMsg.map{makeWith(carrier: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case cacheDoingAtHandID:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = BarbViewController.map()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_neverName.map{fieldNative(search: $0)}, encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case mainTransportServerMaximumNetFlag:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = BarbViewController.by()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: factoryFormatId.map{trestleBridge(body: $0)}, encoding: .utf8)!: status])

            //: case getLangCode:
            case user_linkValue:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: serviceGoingPhoneTopDict.map{contributeApplication(component: $0)}, encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case parserMilieuFormat:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_legalDocumentURL.map{numberContact(mirror: $0)}, encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case networkSounderStr:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: constPackageKitPath.map{$0^134}, encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case loggerMeFormat:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cacheAgentPath.map{sizeCore(disappear: $0)}, encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case formatterInformValue:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: user_captureValue.map{centralismCan(point: $0)}, encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case factoryCornerUrl:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                BarbViewController.directFinish()

            //: case apPay:
            case dataPositiveZoneValue:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.doPad(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cacheActivityString.reversed(), encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case parserSupremePathMsg:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.doPad(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cacheActivityString.reversed(), encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case routerActivityToken:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case kTotalTime:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.totalry()

            //: case openNewWebview:
            case k_versionUrl:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    BarbViewController.afterFilter(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case data_domainSecret:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.afterAgent()

            //: case getMicStatus:
            case parserCountoItemToken:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                CreateToday.shared.purchaseInManager { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: main_textDecideData.map{presentTun(due: $0)}, encoding: .utf8)!: auth, String(bytes: routerBenefitName.map{totalOf(global: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case routerDueKey:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                CreateToday.shared.item { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: main_textDecideData.map{presentTun(due: $0)}, encoding: .utf8)!: auth, String(bytes: routerBenefitName.map{totalOf(global: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case constTextPushMode:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                CreateToday.shared.remove { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName, String(bytes: main_textDecideData.map{presentTun(due: $0)}, encoding: .utf8)!: auth, String(bytes: routerBenefitName.map{totalOf(global: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case modelStatusTransportResult:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        PrepareEvery.receive(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        PrepareEvery.explosiveDetectionSystemColor(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case show_usurpProcessingString:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: networkPresentationValue.map{productionFormat(capture: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                BarbViewController.afterFire(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension BarbViewController {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func afterFilter(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = BarbViewController()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        OfFind.object()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func afterFire(_ model: RaceModel) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(serviceExcludeMessage)" + (String(mainSmartFormat.prefix(6)) + "lPush")
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: parserMessageValue.map{grantSystem(kind: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func outsideObject(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func map() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func by() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if BarbViewController.startWindow() || BarbViewController.push() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func startWindow() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(showNeedClearHeritageMessage.replacingOccurrences(of: "confirm", with: "HT") + String(k_barPadMode.prefix(6)))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(k_nurseLogTime.replacingOccurrences(of: "with", with: "P") + "roxy")] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(applicationFlagFieldState) + String(networkDelayEstablishResult))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func push() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(app_darkName.suffix(7)) + String(notiAreaId.prefix(3)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(viewAddMode))) || keyStr.contains((factoryPastDate.replacingOccurrences(of: "since", with: "t"))) || keyStr.contains((helperAgentDate.replacingOccurrences(of: "to", with: "p"))) || keyStr.contains((String(viewItemList))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func directFinish() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    private func showRunTopic() {
        RunTopic.takeItem((String(modelLabelId.suffix(2)) + String(serviceGlassesKey.prefix(6)) + "ter o" + String(showLogFirstState.prefix(8))) + "\"" + (String(mainPositiveSecret)) + "\"" + (String(factoryClearVerticalDict.prefix(4)) + "cont" + String(configProductFileString.suffix(4)) + app_promptToolMessage.replacingOccurrences(of: "since", with: "s")))
    }
    
    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func doPad(productId: String, source: Int = -1, payType: SpaceLibrary, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        RunTopic.nowWith()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        FindDisk.shared.metatarsalArtery(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            RunTopic.model()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    self.showRunTopic()
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.bezzant()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
