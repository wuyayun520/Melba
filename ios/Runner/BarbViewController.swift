
//: Declare String Begin

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let viewObserverTriggerURL:[UInt8] = [0xf9,0xf2,0xfe,0xe8,0xf0,0xf8,0xf3,0xe9,0xb3,0xfa,0xf8,0xe9,0xd8,0xf1,0xf8,0xf0,0xf8,0xf3,0xe9,0xee,0xdf,0xe4,0xc9,0xfc,0xfa,0xd3,0xfc,0xf0,0xf8,0xb5,0xba,0xff,0xf2,0xf9,0xe4,0xba,0xb4,0xc6,0xad,0xc0,0xb3,0xee,0xe9,0xe4,0xf1,0xf8,0xb3,0xff,0xfc,0xfe,0xf6,0xfa,0xef,0xf2,0xe8,0xf3,0xf9,0xa0,0xba,0xef,0xfa,0xff,0xfc,0xb5,0xad,0xb1,0xad,0xb1,0xad,0xb1,0xad,0xb4,0xba]

private func informLoad(result num: UInt8) -> UInt8 {
    return num ^ 157
}

/*: "closeWeb" :*/
fileprivate let showTotalCount:String = "closeWebpurchase date"

/*: "toUrl" :*/
fileprivate let modelViewKey:String = "toUrloriginal device"

/*: "syncAppInfo" :*/
fileprivate let show_receiveName:String = "hidenc"

/*: "typeName" :*/
fileprivate let serviceProtectionPleaseFormat:String = "typeNametrue album post contact data"

/*: "isAuth" :*/
fileprivate let configFacultyAgentMessage:String = "enter exclude require point reduceisAuth"

/*: "isFirst" :*/
fileprivate let showPromptError:String = "isFirststring challenge"

/*: "Please click 'Go' to allow access" :*/
fileprivate let formatterPendingNameData:[UInt8] = [0xd7,0xeb,0xe2,0xe6,0xf4,0xe2,0xa7,0xe4,0xeb,0xee,0xe4,0xec,0xa7,0xa0,0xc0,0xe8,0xa0,0xa7,0xf3,0xe8,0xa7,0xe6,0xeb,0xeb,0xe8,0xf0,0xa7,0xe6,0xe4,0xe4,0xe2,0xf4,0xf4]

private func forwardInstallScreen(net num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "getCameraStatus" :*/
fileprivate let sessionAggressiveName:[Character] = ["g","e","t","C","a","m","e","r","a","S","t","a","t","u","s"]

/*: "Please allow ' :*/
fileprivate let showIndexStartData:String = "for maximum i withPlease"
fileprivate let laterLabId:String = " allow \'"

/*: ' to access your camera in your iPhone's 'Settings-Privacy-Camera' option" :*/
fileprivate let mainElementResult:[UInt8] = [0x5d,0x56,0xaa,0xa5,0x56,0x97,0x99,0x99,0x9b,0xa9,0xa9,0x56,0xaf,0xa5,0xab,0xa8,0x56,0x99,0x97,0xa3,0x9b,0xa8,0x97,0x56,0x9f,0xa4,0x56,0xaf,0xa5,0xab,0xa8,0x56,0x9f,0x86,0x9e,0xa5,0xa4,0x9b,0x5d,0xa9,0x56,0x5d,0x89,0x9b,0xaa,0xaa,0x9f,0xa4,0x9d,0xa9,0x63,0x86,0xa8,0x9f,0xac,0x97,0x99,0xaf,0x63,0x79,0x97,0xa3,0x9b,0xa8,0x97,0x5d,0x56,0xa5,0xa6,0xaa,0x9f,0xa5,0xa4]

fileprivate func productBody(forward num: UInt8) -> UInt8 {
    let value = Int(num) - 54
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "getPhotoStatus" :*/
fileprivate let user_filterInputDate:[Character] = ["g","e","t","P"]
fileprivate let showScreenStatus:String = "hactiontaction"

/*: ' to access your album in your iPhone's 'Settings-Privacy-Album' option" :*/
fileprivate let notiVerbalizeMName:[UInt8] = [0x6e,0x6f,0x69,0x74,0x70,0x6f,0x20,0x27,0x6d,0x75,0x62,0x6c,0x41,0x2d,0x79,0x63,0x61,0x76,0x69,0x72,0x50,0x2d,0x73,0x67,0x6e,0x69,0x74,0x74,0x65,0x53,0x27,0x20,0x73,0x27,0x65,0x6e,0x6f,0x68,0x50,0x69,0x20,0x72,0x75,0x6f,0x79,0x20,0x6e,0x69,0x20,0x6d,0x75,0x62,0x6c,0x61,0x20,0x72,0x75,0x6f,0x79,0x20,0x73,0x73,0x65,0x63,0x63,0x61,0x20,0x6f,0x74,0x20,0x27]

/*: "getMicStatus" :*/
fileprivate let loggerResponseVersion:String = "remote production bar processing timegetMic"
fileprivate let mainIndexContentResult:String = "Statusforward create"

/*: ' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option" :*/
fileprivate let kPassMode:[UInt8] = [0xc5,0xc2,0x96,0x8d,0xc2,0x83,0x81,0x81,0x87,0x91,0x91,0xc2,0x9b,0x8d,0x97,0x90,0xc2,0x8f,0x8b,0x81,0x90,0x8d,0x92,0x8a,0x8d,0x8c,0x87,0xc2,0x8b,0x8c,0xc2,0x9b,0x8d,0x97,0x90,0xc2,0x8b,0xb2,0x8a,0x8d,0x8c,0x87,0xc5,0x91,0xc2,0xc5,0xb1,0x87,0x96,0x96,0x8b,0x8c,0x85,0x91,0xcf,0xb2,0x90,0x8b,0x94,0x83,0x81,0x9b,0xcf,0xaf,0x8b,0x81,0x90,0x8d,0x92,0x8a,0x8d,0x8c,0x87,0xc5,0xc2,0x8d,0x92,0x96,0x8b,0x8d,0x8c]

private func noFlexible(need num: UInt8) -> UInt8 {
    return num ^ 226
}

/*: "Cancel" :*/
fileprivate let dataMicVersion:String = "Canceltrust me file dismiss allow"

/*: "Poor network, loading failed" :*/
fileprivate let configPendingKey:String = "net finish case capturePoor "
fileprivate let loggerOffList:String = "contribute no pleaseork, "
fileprivate let data_soundWarnVersion:String = "ing grant data library"
fileprivate let configToolFormat:String = "decide"
fileprivate let sessionOriginalStatus:String = "aiorigined"

/*: "Refresh" :*/
fileprivate let data_failMode:String = "Refreshsecret original grant"

/*: "Alert" :*/
fileprivate let app_statusValueValue:String = "that selected commandAlert"

/*: "Done" :*/
fileprivate let modelMiddleNameName:String = "agent area tagDone"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let loggerColorMessage:[UInt8] = [0xa,0x36,0x36,0x32,0x16,0x2d,0x2d,0x2e,0x6c,0xc,0x23,0x36,0x2b,0x34,0x27,0x16,0x2d,0x8,0x31,0x6a,0x65,0x30,0x27,0x21,0x2a,0x23,0x30,0x25,0x27,0x65,0x6b]

private func captureActivity(view num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "onPageShow" :*/
fileprivate let data_mTime:String = "onPageenable will as album"

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let cacheDismissCornerList:[UInt8] = [0xab,0xb5,0xb2,0xb8,0xb3,0xab,0xf2,0xb3,0xb2,0x8c,0xbd,0xbb,0xb9,0x8f,0xb4,0xb3,0xab,0xfa,0xfa,0xb3,0xb2,0x8c,0xbd,0xbb,0xb9,0x8f,0xb4,0xb3,0xab,0xf4,0xf5,0xe7]

private func infoFlexible(bridge num: UInt8) -> UInt8 {
    return num ^ 220
}

/*: "onPageHide" :*/
fileprivate let data_removeVersion:[Character] = ["o","n","P","a","g","e","H","i"]
fileprivate let managerArrayMessage:String = "versione"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let show_enableName:[UInt8] = [0xa,0x14,0x13,0x19,0x12,0xa,0x53,0x12,0x13,0x2d,0x1c,0x1a,0x18,0x35,0x14,0x19,0x18,0x5b,0x5b,0x12,0x13,0x2d,0x1c,0x1a,0x18,0x35,0x14,0x19,0x18,0x55,0x54,0x46]

private func colorMinimum(revenue num: UInt8) -> UInt8 {
    return num ^ 125
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BarbViewController.swift
//  OverseaH5
//
//  Created by DouXiu on 2025/9/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit
 
//: class AppWebViewController: UIViewController {
class BarbViewController: UIViewController {
    //: var urlString: String = ""
    var urlString: String = ""
    /// 是否背景透明
    //: var clearBgColor = false
    var clearBgColor = false
    /// 是否全屏
    //: var fullscreen = true
    var fullscreen = true

    //: private var bridge: WebViewJavascriptBridge?
    private var bridge: WebViewJavascriptBridge?

    // Pending JS dialog completion handlers (ensure always-called to avoid WKWebView crash)
    //: private var pendingAlertCompletion: (() -> Void)?
    private var pendingAlertCompletion: (() -> Void)?
    //: private var pendingConfirmCompletion: ((Bool) -> Void)?
    private var pendingConfirmCompletion: ((Bool) -> Void)?
    //: private var pendingPromptCompletion: ((String?) -> Void)?
    private var pendingPromptCompletion: ((String?) -> Void)?

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []
        //: let userControl = WKUserContentController()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: let w = WKWebView(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: w.scrollView.contentInsetAdjustmentBehavior = .never
        w.scrollView.contentInsetAdjustmentBehavior = .never
        //: w.isOpaque = false
        w.isOpaque = false
        //: w.scrollView.bounces = false
        w.scrollView.bounces = false
        //: w.scrollView.alwaysBounceVertical = false
        w.scrollView.alwaysBounceVertical = false
        //: w.scrollView.alwaysBounceHorizontal = false
        w.scrollView.alwaysBounceHorizontal = false
        //: return w
        return w
        //: }()
    }()

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.addSubview(self.webView)
        view.addSubview(self.webView)
        //: var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        //: if fullscreen == false {
        if fullscreen == false {
            //: frame.origin.y = AppConfig.getStatusBarHeight()
            frame.origin.y = OfFind.source()
        }
        //: self.webView.frame = frame
        self.webView.frame = frame

        //: self.addBridgeMethod()
        self.photo()
        //: self.beginStartRequest()
        self.engagement()

        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(bar),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        bar()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        at()
        //: finalizePendingJSHandlersIfNeeded()
        process()
    }

    //: deinit {
    deinit {
        //: removeBridgeMethod()
        thirdClose()
        //: finalizePendingJSHandlersIfNeeded()
        process()
    }

    /// 发起网页请求
    //: private func beginStartRequest() {
    private func engagement() {
        //: if let url = URL(string: urlString) {
        if let url = URL(string: urlString) {
            //: let urlRequest = URLRequest(url: url)
            let urlRequest = URLRequest(url: url)
            //: self.webView.load(urlRequest)
            self.webView.load(urlRequest)
            //: self.clearJSBgColor()
            self.sprocket()
        }
    }

    /// 设置页面为透明
    //: private func clearJSBgColor() {
    private func sprocket() {
        //: guard clearBgColor == true else { return }
        guard clearBgColor == true else { return }
        //: webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
        webView.evaluateJavaScript(String(bytes: viewObserverTriggerURL.map{informLoad(result: $0)}, encoding: .utf8)!) { _, _ in
        }
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: webView.backgroundColor = .clear
        webView.backgroundColor = .clear
        //: webView.scrollView.backgroundColor = .clear
        webView.scrollView.backgroundColor = .clear
        //: webView.scrollView.bounces = false
        webView.scrollView.bounces = false
        //: webView.scrollView.alwaysBounceVertical = false
        webView.scrollView.alwaysBounceVertical = false
        //: webView.scrollView.alwaysBounceHorizontal = false
        webView.scrollView.alwaysBounceHorizontal = false
        //: webView.isOpaque = false
        webView.isOpaque = false
    }

    /// 关闭webview事件
    //: func closeWeb() {
    func totalry() {
        //: if webView.canGoBack {
        if webView.canGoBack {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }

        //: removeBridgeMethod()
        thirdClose()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = AppConfig.currentViewController() {
                if let currentVC = OfFind.object() {
                    //: if currentVC.isKind(of: AppWebViewController.self) {
                    if currentVC.isKind(of: BarbViewController.self) {
                        //: (currentVC as! AppWebViewController).jsEvent_onPageShow()
                        (currentVC as! BarbViewController).bar()
                    }
                }
            }
        }
    }
}

//: extension AppWebViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
extension BarbViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
    //: func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
    func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
        //: return ""
        return ""
    }

    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: print("js call method name = \(message.name), message = \(message.body)")
        // 兼容老事件
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: let type = message.name
            let type = message.name
            //: if type == "closeWeb" {
            if type == (String(showTotalCount.prefix(8))) {
                //: self.closeWeb()
                self.totalry()

                //: } else if type == "toUrl" {
            } else if type == (String(modelViewKey.prefix(5))) {
                //: if let url = message.body as? String {
                if let url = message.body as? String {
                    //: AppWebViewController.openNewWebView(url)
                    BarbViewController.afterFilter(url)
                }
            }
        }
    }

    //: func addBridgeMethod() {
    func photo() {
        //: self.bridge = WebViewJavascriptBridge(self.webView)
        self.bridge = WebViewJavascriptBridge(self.webView)
        //: self.bridge?.setWebViewDelegate(self)
        self.bridge?.setWebViewDelegate(self)
        //: self.bridge?.registerHandler("syncAppInfo", handler: { data, callback in
        self.bridge?.registerHandler((show_receiveName.replacingOccurrences(of: "hide", with: "sy") + "AppInfo"), handler: { data, callback in
            //: print("js call getUserIdFromObjC, data from js is %@", data as Any)
            //: if callback != nil {
            if callback != nil {
                //: if let dic = data as? [String: Any] {
                if let dic = data as? [String: Any] {
                    //: self.handleH5Message(schemeDic: dic) { backDic in
                    self.prevalenceError(schemeDic: dic) { backDic in
                        //: callback?(backDic)
                        callback?(backDic)
                        //: DispatchQueue.main.async {
                        DispatchQueue.main.async {
                            //: self.handAuthOpenURL(dic: backDic)
                            self.pass(dic: backDic)
                        }
                    }
                }
            }
            //: })
        })
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "closeWeb")
        ucController.add(MemoryScale(self), name: (String(showTotalCount.prefix(8))))
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "toUrl")
        ucController.add(MemoryScale(self), name: (String(modelViewKey.prefix(5))))
    }

    //: func removeBridgeMethod() {
    func thirdClose() {
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            //: ucController.removeAllScriptMessageHandlers()
            ucController.removeAllScriptMessageHandlers()
            //: } else {
        } else {
            //: ucController.removeScriptMessageHandler(forName: "closeWeb")
            ucController.removeScriptMessageHandler(forName: (String(showTotalCount.prefix(8))))
            //: ucController.removeScriptMessageHandler(forName: "toUrl")
            ucController.removeScriptMessageHandler(forName: (String(modelViewKey.prefix(5))))
        }
    }

    //: func handAuthOpenURL(dic: [String: Any]) {
    func pass(dic: [String: Any]) {
        //: if let typeName = dic["typeName"] as? String, let isAuth = dic["isAuth"] as? Bool, let isFirst = dic["isFirst"] as? Bool {
        if let typeName = dic[(String(serviceProtectionPleaseFormat.prefix(8)))] as? String, let isAuth = dic[(String(configFacultyAgentMessage.suffix(6)))] as? Bool, let isFirst = dic[(String(showPromptError.prefix(7)))] as? Bool {
            //: if isAuth || isFirst {
            if isAuth || isFirst {
                //: return
                return
            }
            //: var message = "Please click 'Go' to allow access"
            var message = String(bytes: formatterPendingNameData.map{forwardInstallScreen(net: $0)}, encoding: .utf8)!
            //: var needAlert = false
            var needAlert = false
            //: if typeName == "getCameraStatus" {
            if typeName == (String(sessionAggressiveName)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your camera in your iPhone's 'Settings-Privacy-Camera' option"
                message = (String(showIndexStartData.suffix(6)) + laterLabId) + "\(serviceExcludeMessage)" + String(bytes: mainElementResult.map{productBody(forward: $0)}, encoding: .utf8)!

                //: } else if typeName == "getPhotoStatus" {
            } else if typeName == (String(user_filterInputDate) + showScreenStatus.replacingOccurrences(of: "action", with: "o") + "Status") {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your album in your iPhone's 'Settings-Privacy-Album' option"
                message = (String(showIndexStartData.suffix(6)) + laterLabId) + "\(serviceExcludeMessage)" + String(bytes: notiVerbalizeMName.reversed(), encoding: .utf8)!

                //: } else if typeName == "getMicStatus" {
            } else if typeName == (String(loggerResponseVersion.suffix(6)) + String(mainIndexContentResult.prefix(6))) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option"
                message = (String(showIndexStartData.suffix(6)) + laterLabId) + "\(serviceExcludeMessage)" + String(bytes: kPassMode.map{noFlexible(need: $0)}, encoding: .utf8)!
            }

            //: if needAlert {
            if needAlert {
                //: let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)
                let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)

                //: let action1 = UIAlertAction(title: "Cancel", style: .default) { _ in
                let action1 = UIAlertAction(title: (String(dataMicVersion.prefix(6))), style: .default) { _ in
                }
                //: let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                    //: if let url = URL(string: UIApplication.openSettingsURLString) {
                    if let url = URL(string: UIApplication.openSettingsURLString) {
                        //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                        UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    }
                }
                //: alertController.addAction(action1)
                alertController.addAction(action1)
                //: alertController.addAction(action2)
                alertController.addAction(action2)
                //: present(alertController, animated: true)
                present(alertController, animated: true)
            }
        }
    }
}

//: extension AppWebViewController: WKNavigationDelegate, WKUIDelegate {
extension BarbViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: clearJSBgColor()
        sprocket()
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: let alertController = UIAlertController(title: nil, message: "Poor network, loading failed", preferredStyle: .alert)
        let alertController = UIAlertController(title: nil, message: (String(configPendingKey.suffix(5)) + "netw" + String(loggerOffList.suffix(5)) + "load" + String(data_soundWarnVersion.prefix(4)) + configToolFormat.replacingOccurrences(of: "decide", with: "f") + sessionOriginalStatus.replacingOccurrences(of: "origin", with: "l")), preferredStyle: .alert)
        //: let action = UIAlertAction(title: "Refresh", style: .default) { _ in
        let action = UIAlertAction(title: (String(data_failMode.prefix(7))), style: .default) { _ in
            //: self.reloadWebView()
            self.afterAgent()
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: present(alertController, animated: true)
        present(alertController, animated: true)
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func reloadWebView() {
    func afterAgent() {
        //: if self.webView.url != nil {
        if self.webView.url != nil {
            //: self.webView.reload()
            self.webView.reload()
            //: } else {
        } else {
            //: self.beginStartRequest()
            self.engagement()
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

    //: func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
    func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
        //: self.reloadWebView()
        self.afterAgent()
    }

    //: func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
        //: pendingAlertCompletion = completionHandler
        pendingAlertCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(app_statusValueValue.suffix(5))), message: message, preferredStyle: .alert)
        //: let action = UIAlertAction(title: "OK", style: .default) { _ in
        let action = UIAlertAction(title: "OK", style: .default) { _ in
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = OfFind.object() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
            //: } else {
        } else {
            // Fallback to avoid crash if cannot present
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: pendingConfirmCompletion = completionHandler
        pendingConfirmCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(app_statusValueValue.suffix(5))), message: message, preferredStyle: .alert)
        //: let okAction = UIAlertAction(title: "OK", style: .default) { _ in
        let okAction = UIAlertAction(title: "OK", style: .default) { _ in
            //: self.pendingConfirmCompletion?(true)
            self.pendingConfirmCompletion?(true)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(dataMicVersion.prefix(6))), style: .cancel) { _ in
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(okAction)
        alertController.addAction(okAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = OfFind.object() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
            //: } else {
        } else {
            // Fallback default = false
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: pendingPromptCompletion = completionHandler
        pendingPromptCompletion = completionHandler
        //: let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let doneAction = UIAlertAction(title: "Done", style: .default) { _ in
        let doneAction = UIAlertAction(title: (String(modelMiddleNameName.suffix(4))), style: .default) { _ in
            //: let text = alertController.textFields?.first?.text
            let text = alertController.textFields?.first?.text
            //: self.pendingPromptCompletion?(text)
            self.pendingPromptCompletion?(text)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(dataMicVersion.prefix(6))), style: .cancel) { _ in
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(doneAction)
        alertController.addAction(doneAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = OfFind.object() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
            //: } else {
        } else {
            // Fallback default = nil
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
    }

    //: @available(iOS 15.0, *)
    @available(iOS 15.0, *)
    //: func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
    func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
        //: decisionHandler(.grant)
        decisionHandler(.grant)
    }
}

//: extension AppWebViewController {
extension BarbViewController {
    /// Ensure any pending JS dialog completion handlers are executed to avoid WKWebView crash
    //: private func finalizePendingJSHandlersIfNeeded() {
    private func process() {
        //: if let alertCompletion = pendingAlertCompletion {
        if let alertCompletion = pendingAlertCompletion {
            //: alertCompletion()
            alertCompletion()
            //: pendingAlertCompletion = nil
            pendingAlertCompletion = nil
        }
        //: if let confirmCompletion = pendingConfirmCompletion {
        if let confirmCompletion = pendingConfirmCompletion {
            //: confirmCompletion(false)
            confirmCompletion(false)
            //: pendingConfirmCompletion = nil
            pendingConfirmCompletion = nil
        }
        //: if let promptCompletion = pendingPromptCompletion {
        if let promptCompletion = pendingPromptCompletion {
            //: promptCompletion(nil)
            promptCompletion(nil)
            //: pendingPromptCompletion = nil
            pendingPromptCompletion = nil
        }
    }

    /// 通知三方H5刷新金币
    //: func third_jsEvent_refreshCoin() {
    func bezzant() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: loggerColorMessage.map{captureActivity(view: $0)}, encoding: .utf8)!) { data, error in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func bar() {
        //: self.bridge?.callHandler("onPageShow")
        self.bridge?.callHandler((String(data_mTime.prefix(6)) + "Show"))
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: cacheDismissCornerList.map{infoFlexible(bridge: $0)}, encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func at() {
        //: self.bridge?.callHandler("onPageHide")
        self.bridge?.callHandler((String(data_removeVersion) + managerArrayMessage.replacingOccurrences(of: "version", with: "d")))
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: show_enableName.map{colorMinimum(revenue: $0)}, encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
        }
    }
}
